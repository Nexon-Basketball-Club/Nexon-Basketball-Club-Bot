# 토큰이 들어있다. 절대 커밋하지 않는다 — 태블릿에 손으로 둔다.
config.json

# Rhino jar는 받아서 쓴다 (sim/fetch-rhino.sh)
sim/lib/*.jar
sim/out/
# 시뮬레이터용 로컬 설정 (토큰이 들어간다)
sim/config.json

# 스냅샷은 전부 커밋한다. 시나리오가 명부 없이 자기 픽스처를 결정론적으로 만들므로
# 누구든 `npm run db:scenario <이름>` 뒤에 같은 출력을 재현할 수 있다.
